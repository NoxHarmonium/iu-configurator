# iu-configurator E2E Tests

## Dependencies

- Node
- NPM

## Getting Started

1. Install node (see .tool-versions) if not using asdf
2. npm install

## Running Tests

1. Spin up iu-configurator stack (see root level README)

   $ docker compose up --build --watch

2. Run playwright

   $ npm run test
